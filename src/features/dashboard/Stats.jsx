import Stat from './Stat';

function Stats({ bookings, confirmedStays }) {
  // Calc number of bookings
  const numBookings = bookings.length;

  return (
    <>
      <Stat />
    </>
  );
}

export default Stats;
